this repository contains a few fluxer themes.
copy the css and paste it into custom css overrides, which is it the fluxer settings -> look & feel -> Theme -> Custom Theme Tokens -> Custom CSS Overrides
